language: python
python:
  - "2.6"
  - "2.7"

# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script: python tests.py

deploy:
  provider: openshift
  user: "fatsobob@gmail.com"
  password: "NOPE" # can be encrypted
  domain: "estv-byronsamaripa.rhcloud.com"
